name = "Harry"

print(name[0]) # this will print the first character in Harry

names = ["Harry", "Ron", "Hermione"]
print(names[0]) # this will print the first item in the array. Arrays are mutable

coordinate = (10.0, 20.0) # this is an example of a tuple... Notice it uses () and not []. tuples are immutable
print(coordinate[0]) 